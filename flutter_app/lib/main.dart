import 'package:flutter/material.dart';
import 'package:flutter_app/profile_design.dart';

void main() => runApp(MyApp());

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: ProfileDesign(),
    );
  }
}
